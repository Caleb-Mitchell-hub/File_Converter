你是一个资深 Python 开发工程师，目标是帮我实现一个“企业级文档转换工具”，要求如下：

1. **功能范围**
   - Excel <-> PDF
   - Excel -> 图片(PNG/JPG)
   - PDF -> 图片(PNG/JPG)
   - 图片 -> PDF
   - Word <-> PDF
   - 图片OCR -> Excel

2. **技术要求**
   - 必须使用 Python 生态的开源库（PyWin32, pdfplumber, PyMuPDF, Pillow, docx2pdf, pytesseract 等）。
   - Excel 转 PDF 必须保留原有格式（字体、颜色、边框、图表、合并单元格）。
   - PDF 转 Excel 能自动解析表格。
   - 图片OCR 能支持中文识别。
   - 图片输出高清，至少 300 DPI。
   - 所有转换都可批量处理。

3. **架构要求**
   - 提供统一入口接口，例如 `Converter.convert(source_path, target_path)`。
   - 支持批量目录处理。
   - 自动生成输出目录，不覆盖原始文件。
   - 支持日志记录转换过程。
   - 支持异常处理，转换失败不影响其他文件。
   - 代码可扩展，方便后续增加 Word -> 图片、Markdown -> PDF 等新功能。

4. **输出要求**
   - 完整、可执行 Python 代码。
   - 文件结构清晰，函数和类有文档注释。
   - 包含示例调用和批量处理示例。
   - 使用现代 Python 写法（类型注解、with 上下文管理）。
   - 提供 README，说明如何安装依赖和使用。

5. **额外要求**
   - 给出每个功能模块的依赖库。
   - 给出优化建议，例如：大文件如何处理、内存优化。
   - 提供 Windows 和 Linux 可用的解决方案。
   - 如果必须依赖本地 Office，请指出。