# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
python-multipart>=0.0.9    # 文件上传必需

# 配置
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 核心转换依赖
PyMuPDF>=1.23.0           # PDF 解析与渲染
pdfplumber>=0.10.0        # PDF 表格抽取
Pillow>=10.0.0            # 图片处理
openpyxl>=3.1.0           # Excel 读写
python-docx>=1.0.0        # Word 读写
docx2pdf>=0.1.8           # Word -> PDF

# OCR
pytesseract>=0.3.10

# Windows 专用（仅在 Windows 部署时安装）
# pywin32>=306 ; sys_platform == "win32"

# 工具
python-dotenv>=1.0.0
httpx>=0.26.0             # 测试用

# 测试
pytest>=7.4.0
pytest-asyncio>=0.23.0
